{
  "name": "@thallo/builder-proofs",
  "version": "1.0.0",
  "private": true,
  "description": "Browser proofs of the visual builder's structural editing (visual builder spec §5.6): the Design page against captured API fixtures, in Chromium.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "^24.0.0"
  }
}
